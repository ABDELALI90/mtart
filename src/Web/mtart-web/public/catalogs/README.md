# PDF catalogs

Place your real MT ART PDF catalogs here (e.g. `mtart-zellige-collection-en.pdf`). The `/catalogs` page
(`src/pages/CatalogsPage.tsx`) lists catalog cards backed by CMS data once the Content service implements
catalog management (Phase 5/7); until then it renders a polished placeholder list clearly marked as such.
